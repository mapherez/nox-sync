services:
  nox-sync:
    build:
      context: ./backend
    image: nox-sync:dev
    container_name: nox-sync-dev
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      NOX_SYNC_ADDR: ":8080"
      NOX_SYNC_DATA_DIR: "/data"
    volumes:
      - ./data:/data
